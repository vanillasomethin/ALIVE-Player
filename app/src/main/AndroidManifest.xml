<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.alive.player">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Lets a self-update commit with USER_ACTION_NOT_REQUIRED once this app is its
         own installer-of-record (first successful self-update onward, Android 12+).
         Normal permission — granted at install, no runtime prompt. -->
    <uses-permission android:name="android.permission.UPDATE_PACKAGES_WITHOUT_USER_ACTION" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!--
        Without these, Play Console assumes a touchscreen is required (the Android
        default) and filters this app out of the Android TV / Google TV catalog
        entirely — it never shows up as installable on TV devices even though the
        manifest already has a LEANBACK_LAUNCHER intent-filter and TV banner.
    -->
    <uses-feature android:name="android.software.leanback" android:required="true" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:name=".AliveApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:banner="@drawable/ic_tv_banner"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <activity
            android:name="com.alive.player.ui.PairingActivity"
            android:exported="true"
            android:resizeableActivity="false"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize"
            android:theme="@style/Theme.Alive.Pairing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
            <!--
                HOME claim: device owner silently registers this as the persistent
                preferred Home app (OwnerSetup.kt) so the device boots straight into
                the player, independent of OEM autostart/battery restrictions.
            -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!--
            configChanges declared so requestedOrientation rotates without recreating the
            Activity — the foreground service and ExoPlayer keep running uninterrupted.

            singleTask: every relaunch (launcher icon, Pairing forward, OTA relaunch)
            must REUSE the existing instance instead of stacking a new one in the task.
            Three stacked instances were observed on a HiSilicon panel (2026-08-14);
            a buried instance's stale poller kicked engine.startLoop() every 5s, so the
            screen replayed the first seconds of playlist item 0 forever. Relaunching
            also clears anything stacked above (e.g. a forgotten Settings screen).
        -->
        <activity
            android:name="com.alive.player.ui.PlaybackActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:resizeableActivity="false"
            android:theme="@style/Theme.Alive.Playback"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize" />

        <activity
            android:name="com.alive.player.settings.SettingsActivity"
            android:exported="false"
            android:resizeableActivity="false"
            android:theme="@style/Theme.Alive.Settings" />

        <service
            android:name="com.alive.player.service.PlaybackForegroundService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Runs in its own process so it keeps working even if the main process's
             main thread fully freezes (ANR) — see WatchdogService kdoc. -->
        <service
            android:name="com.alive.player.service.WatchdogService"
            android:exported="false"
            android:process=":watchdog"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Cross-process crash watchdog for unattended kiosk playback" />
        </service>

        <service
            android:name="com.alive.player.network.AliveMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <receiver
            android:name="com.alive.player.receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.alive.player.worker.UpdateInstallReceiver"
            android:exported="false" />

        <!-- Relaunches playback right after an OTA self-update replaces the package —
             without it a successful install leaves a dead screen until reboot.
             MY_PACKAGE_REPLACED is a protected broadcast only the system can send. -->
        <receiver
            android:name="com.alive.player.receiver.PackageReplacedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Device Owner: lets the player silently claim HOME and silently install
             OTA updates (PackageInstaller, no confirm dialog, API 31+). Enrolled via
             zero-touch QR provisioning — see PROVISIONING.md. -->
        <receiver
            android:name="com.alive.player.admin.AliveDeviceAdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
                <action android:name="android.app.action.PROFILE_PROVISIONING_COMPLETE" />
            </intent-filter>
        </receiver>

        <!-- WorkManager workers -->
        <provider
            android:name="androidx.work.impl.WorkManagerInitializer"
            android:authorities="${applicationId}.workmanager-init"
            android:exported="false"
            tools:node="remove" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup" />
        </provider>
    </application>
</manifest>
